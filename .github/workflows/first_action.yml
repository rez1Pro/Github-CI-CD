name: "First-action"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["dev","main"]

jobs:
  First-runner:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Testing something
        run: echo "Hello developer"
        shell: bash